import React from "react";
import { View, Text, StyleSheet } from "react-native";

const EatPassScreen = (props) => {
    return (
        <View>
            <Text>Eat Pass page</Text>
        </View>
    );
};

const styles = StyleSheet.create();

export default EatPassScreen;
